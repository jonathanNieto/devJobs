<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://bootswatch.com/4/sketchy/bootstrap.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.8.2/css/all.css">
    <link href="https://fonts.googleapis.com/css?family=Rubik:400,700,900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Poppins:400,500,600,700&display=swap" rel="stylesheet">
    {{!-- trix --}}
    <link rel="stylesheet" type="text/css" href="/trix/trix.css">
    <link rel="stylesheet" href="/css/app.css">
    <link rel="stylesheet" href="/css/animate.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/util.css">
    <title>{{pageName}}</title>
</head>

<body>
    <div class="navbar navbar-dark bg-primary">
        <a href="/" class="navbar-brand ml-5">
            <h1 class="fw-700">devJobs</h1>
        </a>
        <div class="d-flex justify-content-end align-items-center">
            {{#if bar}}
            <form class="form-inline mr-5" action="/search" method="POST">
                <div class="input-group">
                    <input type="search" name="search" class="form-control" placeholder="buscar...">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-secondary text-white"><i class="fas fa-search"></i></button>
                    </div>
                </div>
            </form>
            {{/if}}
            {{#if image}}
                <img class="img" src="{{image}}" alt="imagen">
            {{/if}}
            {{#if logout}}
            <div class="btn-group dropleft mr-5">
                <a class="dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Hola, <span class="text-info">{{name}}</span>
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {{#if admin}}
                    <a class="dropdown-item" href="/edit-profile"><i class="fas fa-user-edit"></i>&nbsp;Editar perfil</a>
                    <a class="dropdown-item" href="/admin"><i class="fas fa-solar-panel"></i>&nbsp;Administrar vacantes</a>
                    <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt"></i>&nbsp;Cerrar Sesión</a>
                    </div>
                    {{else}}
                    <a class="dropdown-item disabled" href="/edit-profile"><i class="fas fa-user-edit"></i>&nbsp;Editar perfil</a>
                    <a class="dropdown-item disabled" href="/admin"><i class="fas fa-solar-panel"></i>&nbsp;Administrar vacantes</a>
                    <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/login"><i class="fas fa-sign-in-alt"></i>&nbsp;Iniciar Sesión</a>
                    </div>
                    {{/if}}
            </div>
            {{/if}}
        </div>
    </div>
    <header class="container mt-3">
        <div class="d-flex justify-content-between">
            <div>
                <h2>{{pageName}}</h2>
                {{#if tagLine}}
                <p class="text-primary">{{tagLine}}</p>
                {{/if}}
            </div>
            <div>
                {{#if btn}}
                <a href="/jobs/new" class="btn btn-info mt-3 mr-5">Nueva vacante</a>
                {{/if}}
            </div>
        </div>
    </header>

    <div class="container">
        {{#if messages}}
        <div id="alerts">
            {{#showAlerts messages}}
            {{/showAlerts}}
        </div>
        {{/if}}
        {{{body}}}
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    {{!-- trix --}}
    <script type="text/javascript" src="/trix/trix.js"></script>
    <script src="/dist/bundle.js"></script>
</body>

</html>